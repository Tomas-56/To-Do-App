<UserControl x:Class="ToDoWPF.View.TaskPanelView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:ToDoWPF.View"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800">
    <Grid Background="{StaticResource brown-Background-Brush}">
        
        <Grid.RowDefinitions>
            <RowDefinition Height="120" />
            <RowDefinition Height="10"/>
            <RowDefinition/>
        </Grid.RowDefinitions>
        
        <StackPanel  Grid.Row="0" Orientation="Horizontal" HorizontalAlignment="Left" VerticalAlignment="Center">
            <Label Content="ToDo" FontSize="42" Width="Auto" Foreground="{StaticResource darkBrown-Text-Brush}" FontFamily="{StaticResource FontFromDictionary}" ></Label>
            <Button Margin="200,0,0,0" Content="+Add">
                
            </Button>
            <Button Style="{DynamicResource ButtonStyle1}" Content="-Remove">
                
            </Button>
        </StackPanel>

        <Rectangle Grid.Row="1" Fill="{StaticResource brightBrown-Overlay-Brush}">
            
        </Rectangle>
        
        <Rectangle Grid.Row="2" Fill="{StaticResource whiteAmber-Background-Brush}">
            
        </Rectangle>
        
    </Grid>
</UserControl>
